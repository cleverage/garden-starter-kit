# ENVIRONNEMENT GLOBAL
# -----------------------------------------------------------------------------
# Pour être sûr que le fichier .gitignore n'est jamais ignoré lui-même.
!.gitignore

# SASS
# -----------------------------------------------------------------------------
# Ignorer le cache de Sass/Compass
.sass-cache

# Ignorer la configuration interne de Bundler
.bundle

# Ignorer les gems installées avec le projet
# N'hésitez pas à adapter le nom du dossier à vos propres habitudes
.gems


# MODULES TIERS
# -----------------------------------------------------------------------------
# Ignorer les modules et logs nodes
node_modules
logs
*.log
npm-debug.log*

# Ignorer les bibliothèques et fichiers temporaires gérés via JSPM
jspm_packages

# Ignorer le cache nproxy
nproxy-cache
# Ignorer la conf du serveur mandataire dans nproxy
# .gsk/conf/nproxy-auth.json

# Ignorer les identifiants du serveur sftp
.ftppass

# PROJET
# -----------------------------------------------------------------------------
# Ignorer les bibliothèques JS tierces placées dans les sources
# src/js/lib

# Ignorer les bibliothèques Sass tierces placées dans les sources
# src/sass/lib

# Ignorer le build
build

# Ignorer le fichier contenant tous les symbols svg regroupés
src/assets/svg/symbols.svg
